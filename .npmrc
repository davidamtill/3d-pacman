# Ensure consistent package-lock.json
package-lock=true

# Use exact versions
save-exact=true

# Increase install timeout for slower connections
fetch-timeout=60000

# Progress indicator
progress=true
